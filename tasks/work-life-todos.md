I want to edit my `todo` function to open `files/work.md` when on a hostname that has "ascend" in it, otherwise `files/life.md`. ideally we should probably also set a variable toward the top for "MACHINE_TYPE", which is set to WORK or LIFE (we may add more later). then in `todo`, just use this to determine which file to open.

keep it simple and update my .bash_aliases to achieve this
